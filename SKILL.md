---
name: material-to-deliverable
description: 将本地真实资料整理为可核验的文字交付包。用于用户希望基于文件夹中的材料制作汇报、方案、复盘、调研或课程展示，并需要任务澄清、材料索引、来源追踪、事实审核和最终交付文件时。不用于虚构演示、无来源写作、单文件润色或自动脱敏。
license: MIT
compatibility: 需要能够读取和写入本地文件、逐轮询问用户并运行 Python 3 的 Agent。无需联网。
metadata:
  version: "0.1.0"
---

# Material to Deliverable

把 `00_input/` 中的真实资料整理成可检查、可追溯、可交付的文字文件。

## 启动条件

仅在同时满足以下条件时执行。

- 用户提供或指定了本地项目目录。
- `00_input/` 中至少有一份真实材料。
- 用户希望得到汇报、方案、复盘、调研或课程展示之一。

目录不明确时，只问项目目录。交付类型不明确时，只问交付类型。没有真实材料时停止，不生成示例或替代材料。

## 每次都要读取

开始处理前完整读取：

- [证据与安全规则](references/evidence-and-safety.md)
- [文件合同](references/file-contract.md)

确认交付类型以后，再读取 [交付类型规则](references/deliverable-types.md) 中对应的一节。

## 工作流程

### 1. 确认项目与初始化

项目尚未初始化时，从本 Skill 的安装目录调用脚本，并传入明确的项目路径：

```bash
python3 <skill-directory>/scripts/init_project.py /absolute/path/to/project
```

不要假设用户项目当前目录中存在 `scripts/`。先根据本文件位置解析 `<skill-directory>`，再执行脚本。

如果 `01_working/` 已存在，读取已有状态并继续，不重新初始化，不覆盖文件。

### 2. 检查输入与敏感信息

列出 `00_input/` 中全部文件，记录无法读取或来源不明的文件。不要修改、移动或重命名原始材料。

先做敏感信息检查。发现姓名、个人联系方式、账号、客户资料、合同、财务、健康、教育记录、凭证、内部路径或其他可能受限的信息时：

1. 只在 `02_material-index.md` 记录风险类型与文件位置，不复制敏感值。
2. 立即暂停。
3. 请用户确认其有权处理这些材料，或请用户提供脱敏副本。

未经用户确认，不继续提纲、写作和交付。

### 3. 澄清任务

每次只问一个会改变成品的问题，最多七个。优先确认接收者、使用场景、希望促成的决定、截止时间、交付类型、必须保留和禁止出现的内容。

用户回答“不知道”时，写入待确认事项，不替用户补答案。将结果写入 `01_task-brief.md`，展示摘要，等待用户确认。

### 4. 建立材料索引与缺口

逐份材料填写 `02_material-index.md`，分清：

- 来源能够直接支持的事实
- 当事人或机构自述
- 访谈、反馈或局部观察
- 用户本人判断
- 待确认信息

将冲突、缺失、无法读取、需要脱敏和证据不足的内容写入 `03_gaps-and-conflicts.md`。少量材料不能扩大为普遍结论。

阻断性缺口仍存在时继续提问或停止，不生成主成品。

### 5. 创建带来源提纲

根据交付类型创建 `04_source-backed-outline.md`。每个部分必须写清要回答的问题、使用的材料、允许得出的结论、用户判断、待确认项和验收方式。

展示提纲摘要与来源覆盖情况，等待用户确认。未确认前不写主成品。

### 6. 生成成品与来源索引

根据确认后的提纲生成：

- `05_main-deliverable.md`
- `06_one-page-summary.md`
- `07_source-index.md`

每个关键事实都要在来源索引中对应到具体文件和位置。不得补写材料中没有的数字、人物、案例、引语或结果。个人判断必须与来源事实分开。

### 7. 回到原始材料审核

重新读取 `00_input/`，不要只根据刚生成的成品自我检查。将事实、范围、逻辑、隐私、任务一致性和表达问题写入 `08_audit-report.md`。

只修改能够由现有材料明确修复的问题。需要用户判断的内容保持待确认。

### 8. 整理交付包

只有审核中没有阻断问题时，才把通过审核的内容写入：

```text
02_delivery/
  main-deliverable.md
  one-page-summary.md
  source-index.md
  readme.md
```

`02_delivery/` 不得包含原始材料、提示词、聊天记录、内部草稿或敏感值。最后仍从本 Skill 的安装目录运行：

```bash
python3 <skill-directory>/scripts/validate_project.py /absolute/path/to/project --stage delivery
```

验证脚本通过后，仍要提醒用户做最终人工放行。脚本只检查文件结构与明显残留，不能证明事实正确。

## 停止条件

遇到以下任一情况就停止，并说明缺少什么。

- `00_input/` 为空或材料无法读取。
- 用户未确认敏感材料可以继续处理。
- 接收者、交付类型或核心目标仍不明确。
- 当前材料无法支持主结论。
- 用户尚未确认任务单或提纲。

不要用虚构材料、联网搜索或常识推断绕过停止条件，除非用户另行明确授权新的资料来源。

## 输出语言

默认跟随用户当前使用的语言。文件名保持英文，便于跨环境使用；文件内容使用用户语言。
