# 参与项目

感谢你帮助改进 Material to Deliverable。

## 提交问题

Issue 请包含：

- 使用的 Agent 与版本
- 触发 Skill 的原始请求
- 输入材料类型，不要上传真实敏感内容
- 预期行为与实际行为
- 是否能够在 `tests/fixtures/` 的合成材料中复现

不要提交客户、患者、学生、员工、合同、账号、凭证或其他真实受限资料。

## 提交改动

1. 保持 Skill 只处理真实材料到文字交付包这一件事。
2. 新规则必须对应一个能够复现的问题。
3. 新增交付类型时，同时更新 `references/deliverable-types.md` 和行为验收场景。
4. 修改文件合同或脚本时，补充对应单元测试。
5. 不引入网络、付费服务和第三方依赖，除非提案已经说明必要性和边界。

提交前运行：

```bash
python3 -m unittest discover -s tests -p 'test_*.py'
```

并确认公开文档与代码中没有未处理的占位符、本机真实路径、私人邮箱、密钥或 token。`assets/project-templates/` 中用于运行时填写的 `{{...}}` 占位符，以及测试夹具中的明确合成值除外。

## 设计原则

- 原始材料只读
- 证据不足就停
- 敏感信息先确认
- 脚本只做能够确定验证的事情
- 不把一个测试案例变成所有用户的固定规则
